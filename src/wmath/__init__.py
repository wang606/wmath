# __all__ = ['Meta', 'number_theory', 'fraction', 'polynomial', 'matrix']
from meta import *
from number_theory import *
from fraction import *
from polynomial import *
from matrix import *
"""
if not hasattr(Meta.ONE, 'Fraction'):
    Meta.ONE.Fraction = Fraction(1, 1)
if not hasattr(Meta.ZERO, 'Fraction'):
    Meta.ZERO.Fraction = Fraction(0, 1)
"""
